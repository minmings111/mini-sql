#include "repl.h"

int run_repl(void) {
    return 0;
}

int process_input_line(const char *line) {
    (void) line;
    return 0;
}

int is_exit_command(const char *line) {
    (void) line;
    return 0;
}
